import { cleanup, fireEvent, render } from '@testing-library/react';
import ChangeUserName from './ChangeDisplayName';

describe('componenets', () => {
	describe('access-denied', () => {
		it('properly loads the component', () => {
			// const {} = render(<ChangeUserName />,);
		});
	});
});
